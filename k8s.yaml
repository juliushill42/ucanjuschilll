# ============================================================
# UCANJUSCHILL — Kubernetes Manifests
# Julius Cameron Hill IP
# All services: Go API, Rust Ingest, AI Worker, Redis, Nginx
# ============================================================

apiVersion: v1
kind: Namespace
metadata:
  name: ucanjuschill

---
# ── Go API Deployment ──
apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-api
  namespace: ucanjuschill
spec:
  replicas: 2
  selector:
    matchLabels:
      app: go-api
  template:
    metadata:
      labels:
        app: go-api
    spec:
      containers:
        - name: go-api
          image: us-east1-docker.pkg.dev/gen-lang-client-0695165560/juschill-repo/go-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: GIN_MODE
              value: release
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: jwt-secret
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: redis-url
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20

---
apiVersion: v1
kind: Service
metadata:
  name: go-api
  namespace: ucanjuschill
spec:
  selector:
    app: go-api
  ports:
    - port: 8080
      targetPort: 8080

---
# ── Rust Ingest (RTMP) ──
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingest
  namespace: ucanjuschill
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ingest
  template:
    metadata:
      labels:
        app: ingest
    spec:
      containers:
        - name: ingest
          image: us-east1-docker.pkg.dev/gen-lang-client-0695165560/juschill-repo/ingest:latest
          ports:
            - containerPort: 1935
          env:
            - name: RTMP_BIND
              value: "0.0.0.0:1935"
            - name: GO_API_URL
              value: "http://go-api:8080"
            - name: HLS_BUCKET
              value: "gs://gen-lang-client-0695165560-hls"
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi

---
apiVersion: v1
kind: Service
metadata:
  name: ingest
  namespace: ucanjuschill
spec:
  type: LoadBalancer
  selector:
    app: ingest
  ports:
    - port: 1935
      targetPort: 1935
      name: rtmp

---
# ── AI Worker ──
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-worker
  namespace: ucanjuschill
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-worker
  template:
    metadata:
      labels:
        app: ai-worker
    spec:
      containers:
        - name: ai-worker
          image: us-east1-docker.pkg.dev/gen-lang-client-0695165560/juschill-repo/ai-worker:latest
          ports:
            - containerPort: 8001
          env:
            - name: GO_API_URL
              value: "http://go-api:8080"
            - name: VERTEX_PROJECT
              value: "gen-lang-client-0695165560"
            - name: VERTEX_REGION
              value: "us-east1"
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: openai-api-key
            - name: ELEVENLABS_API_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: elevenlabs-api-key
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: 2000m
              memory: 2Gi

---
# ── AI Worker Service ──
apiVersion: v1
kind: Service
metadata:
  name: ai-worker
  namespace: ucanjuschill
spec:
  selector:
    app: ai-worker
  ports:
    - port: 8001
      targetPort: 8001

---
# ── Redis ──
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: ucanjuschill
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          command: ["redis-server", "--appendonly", "yes"]
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            - name: redis-data
              mountPath: /data
      volumes:
        - name: redis-data
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: ucanjuschill
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379

---
# ── Nginx (CDN + SSL) ──
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: ucanjuschill
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
            - containerPort: 443
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi

---
apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: ucanjuschill
  annotations:
    cloud.google.com/load-balancer-type: External
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
      name: http
    - port: 443
      targetPort: 443
      name: https

---
# ── Horizontal Pod Autoscaler ──
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: go-api-hpa
  namespace: ucanjuschill
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: go-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
